import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

public class ImparMelhor extends Actor
{
    int aleatorio;
    String nome = "";
    public void act()
    {
        //if (Greenfoot.isKeyDown("i")) {
            //aleatorio = Greenfoot.getRandomNumber(2);
            //Greenfoot.ask("digite um numero par");
            //nome = Greenfoot.ask("digite um numero par");
            //getWorld().showText("Voce digitou:" + nome, 500, 320);
            //if (aleatorio == 1) {
                //getWorld().showText("Voce Ganhou", 500, 300);
                
                //getWorld().showText("" + aleatorio, 500, 340);
            //}
            //if (aleatorio == 0) {
               // getWorld().showText("Voce Perdeu", 500, 300);
                
               // getWorld().showText("" + aleatorio, 500, 340);
           // }
        }
    }
//}
